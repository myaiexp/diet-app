{
  "name": "diet-app",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev:tunnel": "ssh -N -L 5433:localhost:5432 vps",
    "build": "pnpm -r build",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22"
  }
}
