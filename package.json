{
  "name": "diet-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:tunnel": "ssh -N -L 5433:localhost:5432 vps"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.9.3"
  },
  "comments": {
    "overrides": "Drops drizzle-kit's vestigial, deprecated @esbuild-kit/esm-loader (it loads TS configs via tsx, never imports esbuild-kit). See CLAUDE.md. Remove once drizzle-kit >0.31.10 drops the dead dep."
  },
  "overrides": {
    "drizzle-kit": {
      "@esbuild-kit/esm-loader": "npm:tsx@^4"
    }
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@11.16.0"
}
